@AGENTS.md

## Claude Code

- Use the project-scoped `catalogready` server from `.mcp.json` for audits.
- Prefer MCP tools over reimplementing catalog rules in a prompt.
- Ask before enabling live crawling, external model calls, or merchant-system writes.

